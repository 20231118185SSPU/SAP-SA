---
title: "TOOLS.md 模板"
summary: "TOOLS.md 工作区模板"
read_when:
  - 手动引导工作区
---

# TOOLS.md - 本地备注

技能定义了工具_如何_工作。此文件记录的是_你的_具体信息 —— 你的环境独有的那些东西。

## 外部 AI 工具

当 SA 自身能力不足以完成某项子任务时，可以通过 `Bash` 调用以下外部 AI 工具作为补充：

### Claude Code（Anthropic CLI）

- 命令：`claude`
- 适合：复杂代码分析、大规模重构、深度推理、需要长上下文的编程任务
- 用法示例：`claude -p "分析这段代码的性能瓶颈" --allowedTools Read Edit Bash`
- 注意：Claude Code 有自己的工具集和权限体系，通过 `-p` 传入提示时它会非交互式执行

### Codex（OpenAI CLI）

- 命令：`codex`
- 适合：快速代码生成、广泛的知识问答、多轮对话式编程辅助
- 用法示例：`codex -q "用 Python 实现一个快速排序"`
- 注意：Codex 同样有自己的沙箱和权限模型

### 使用原则

- 优先用 SA 自身的工具完成任务；只有当任务超出 SA 能力范围或需要第二意见时才调用外部 AI
- 通过 `SubAgent` + `Bash` 调用外部 AI，避免其输出直接污染主上下文
- 外部 AI 的输出需要你自己验证后再呈现给同学，不要盲目转发

## 本地环境备注

在此记录你的环境独有的信息：

- 设备名称、路径别名
- SSH 主机和配置
- 任何环境相关的信息

## 为什么要分开？

技能是共享的，你的配置是你自己的。分开存放意味着你可以更新技能而不丢失自己的备注，也可以分享技能而不泄露你的基础设施信息。

---

在此添加任何有助于你工作的内容。这是你的速查手册。
