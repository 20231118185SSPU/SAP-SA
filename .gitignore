# Rust build output.
/target/

# Editor / OS noise.
.DS_Store
Thumbs.db

# Local configuration containing secrets (API keys, etc.).
sa.toml
claw.toml

# Local memory files (often sensitive; still kept local).
MEMORY.md
memory/

# Local agent state (persistent memory, caches, etc.).
.sa/
.claw/

# Optional local logs.
*.log
